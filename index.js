import React from 'react'
import { AppRegistry } from 'react-native';
import App from './src/App';

const ReactNativeRedux = props => (
    <App/>
)

AppRegistry.registerComponent('ReactNativeRedux', () => ReactNativeRedux);
